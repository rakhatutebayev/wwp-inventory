# Быстрый старт тестирования

## Текущий статус

✅ Зависимости мобильного приложения установлены  
🚀 Expo сервер запускается...

## Что делать дальше

### 1. Дождитесь запуска Expo

После запуска вы увидите:
- QR-код в терминале
- Меню с опциями (нажмите `?` для помощи)

### 2. Подключите устройство

**Вариант A: Реальное устройство (рекомендуется)**
1. Установите **Expo Go** на телефон:
   - iOS: [App Store](https://apps.apple.com/app/expo-go/id982107779)
   - Android: [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)
2. Отсканируйте QR-код из терминала
3. Дождитесь загрузки приложения

**Вариант B: Эмулятор/Симулятор**
- Нажмите `a` для Android эмулятора
- Нажмите `i` для iOS симулятора (только macOS)
- Нажмите `w` для веб-браузера

### 3. Настройте API URL (если тестируете на реальном устройстве)

1. Найдите IP-адрес вашего компьютера:
   ```bash
   # macOS/Linux
   ifconfig | grep "inet " | grep -v 127.0.0.1
   
   # Windows
   ipconfig
   ```

2. Откройте `mobile/src/services/api.js`
3. Замените `localhost` на ваш IP-адрес:
   ```javascript
   const API_BASE_URL = 'http://192.168.1.XXX:8000'
   ```
4. Перезапустите Expo (нажмите `r` в терминале)

### 4. Убедитесь, что backend запущен

Backend должен быть доступен на `http://localhost:8000` (или на вашем IP для реального устройства).

Запустите backend если он не запущен:
```bash
cd backend
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### 5. Начните тестирование

#### Базовые проверки:

1. **Вход в систему**
   - Откройте приложение
   - Введите учетные данные
   - Проверьте переход на главный экран

2. **Список устройств**
   - Проверьте отображение устройств
   - Попробуйте поиск

3. **Детали устройства**
   - Нажмите на устройство
   - Проверьте отображение информации
   - Проверьте историю перемещений

4. **Сканирование**
   - Перейдите на вкладку "Сканировать"
   - Разрешите доступ к камере
   - Попробуйте отсканировать QR-код

5. **Перемещения**
   - Откройте детали устройства
   - Нажмите "Переместить"
   - Выберите локацию и создайте перемещение

6. **История и отчеты**
   - Проверьте вкладку "Перемещения"
   - Проверьте вкладку "Отчеты"

## Полезные команды в Expo

- `r` - перезагрузить приложение
- `m` - переключить меню
- `?` - показать помощь
- `a` - открыть на Android
- `i` - открыть на iOS
- `w` - открыть в браузере
- `Ctrl+C` - остановить сервер

## Решение проблем

### Приложение не подключается
- Проверьте, что устройство и компьютер в одной Wi-Fi сети
- Проверьте настройки файрвола
- Попробуйте использовать туннель: нажмите `s` в Expo меню

### Ошибки API
- Проверьте, что backend запущен
- Проверьте правильность API URL
- Проверьте логи backend в терминале

### Ошибки камеры
- Проверьте разрешения в настройках устройства
- Перезапустите приложение

## Подробные инструкции

См. файл `MOBILE_TESTING.md` для детальных тестовых сценариев.



