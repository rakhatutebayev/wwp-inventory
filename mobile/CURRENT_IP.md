# 📱 Текущий IP адрес для подключения

## ✅ Новый IP адрес: `192.168.0.128`

**Старый IP был:** `192.168.11.157`  
**Новый IP:** `192.168.0.128`

## 🚀 URL для подключения в Expo Go:

```
exp://192.168.0.128:8081
```

## 📝 Как узнать текущий IP:

```bash
ifconfig | grep "inet " | grep -v 127.0.0.1 | head -1 | awk '{print $2}'
```

Или проще:
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

## 🔧 Если IP меняется часто:

Можно использовать tunnel режим Expo, который не зависит от IP:

```bash
cd mobile
npx expo start --tunnel --android
```

Tunnel создаст постоянный URL типа `exp://xxx.tunnel.exp.direct:80`, который не меняется при смене IP.



