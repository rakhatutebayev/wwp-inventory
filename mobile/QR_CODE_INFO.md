# 📱 QR код для Expo Go

## 🚀 Быстрый способ:

### Вариант 1: Использовать HTML генератор

Я создал HTML файл `generate-qr.html`:

1. **Откройте файл в браузере:**
   ```bash
   cd mobile
   open generate-qr.html
   ```
   Или просто откройте файл `mobile/generate-qr.html` в любом браузере

2. **Введите URL** (или используйте предложенный)
3. **QR код сгенерируется автоматически**
4. **Отсканируйте QR код** в Expo Go

### Вариант 2: Использовать скрипт (автоматически запускает Expo)

```bash
cd mobile
./start-with-qr.sh
```

Этот скрипт:
- Запустит Expo в tunnel режиме
- Получит tunnel URL
- Откроет HTML с QR кодом
- Покажет URL для ручного ввода

### Вариант 3: Использовать терминал Expo

Когда запускаете Expo, в терминале уже есть QR код:

```bash
cd mobile
npx expo start --tunnel --android
```

QR код будет прямо в терминале - просто отсканируйте его.

## 📋 Текущий URL:

**Для LAN режима:** `exp://192.168.0.128:8081`

**Для tunnel режима:** Будет показан в терминале при запуске (типа `exp://xxx.tunnel.exp.direct:80`)

## 💡 Рекомендация:

**Используйте tunnel режим**, так как macOS блокирует прямой доступ к порту:
```bash
npx expo start --tunnel --android
```

QR код будет прямо в терминале, или используйте `generate-qr.html` для создания QR кода с любым URL.



