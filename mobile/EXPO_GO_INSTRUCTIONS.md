# 📱 Инструкция: Запуск через Expo Go

## ✅ Текущий статус:
- ✅ Эмулятор запущен
- ✅ Expo сервер работает на порту 8081
- ✅ IP адрес: 192.168.11.157
- ✅ Backend работает на порту 8000

## 🚀 Шаги для запуска:

### 1. Откройте Expo Go на эмуляторе

На эмуляторе найдите и откройте приложение **"Expo Go"** (оранжевая иконка).

Если не видите иконку:
- Прокрутите список приложений вниз
- Или используйте поиск приложений

### 2. Подключитесь к проекту

В Expo Go выберите один из способов:

#### Способ А: Ввести URL вручную (рекомендуется)
1. Нажмите кнопку **"Enter URL manually"** (или "Ввести URL вручную")
2. Введите следующий URL:
   ```
   exp://192.168.11.157:8081
   ```
3. Нажмите **"Connect"** (или "Подключиться")

#### Способ Б: Отсканировать QR код
1. В терминале где запущен Expo должен быть QR код
2. В Expo Go нажмите **"Scan QR code"**
3. Используйте камеру эмулятора для сканирования

### 3. Дождитесь загрузки

После подключения:
- Первая загрузка может занять 1-2 минуты
- Вы увидите экран загрузки
- Затем должен появиться экран входа в приложение WWP Inventory

## ✅ Что вы должны увидеть:

После успешного подключения:
- Экран входа в систему WWP Inventory
- Поля для ввода логина и пароля
- Кнопка "Войти"

## 🔧 Если не работает:

### Проблема: Expo Go не открывается
```bash
# Перезапустите Expo Go
adb shell am force-stop host.exp.exponent
adb shell monkey -p host.exp.exponent 1
```

### Проблема: Не подключается к серверу
1. Проверьте, что Expo запущен:
   ```bash
   lsof -ti:8081
   ```
   
2. Если не запущен, запустите:
   ```bash
   cd mobile
   npm run android
   ```

3. Попробуйте другой IP адрес:
   ```bash
   ifconfig | grep "inet " | grep -v 127.0.0.1
   ```
   Используйте этот IP в URL: `exp://<ваш_ip>:8081`

### Проблема: Ошибка подключения
- Убедитесь, что эмулятор и компьютер в одной сети
- Попробуйте использовать tunnel режим:
  ```bash
  cd mobile
  npx expo start --tunnel --android
  ```

## 📝 Быстрые команды:

```bash
# Проверить статус
cd mobile && ./check-app.sh

# Перезапустить Expo
lsof -ti:8081 | xargs kill -9
cd mobile && npm run android

# Открыть Expo Go на эмуляторе
adb shell monkey -p host.exp.exponent 1

# Подключиться к приложению
adb shell am start -a android.intent.action.VIEW -d "exp://192.168.11.157:8081"
```

## 🎯 Готово!

После подключения приложение должно работать на эмуляторе через Expo Go!



