# 🚀 Быстрый старт Android эмулятора

## ✅ Что уже сделано:
- ✅ Android Studio установлен
- ✅ Переменные окружения настроены в `~/.zshrc`

## 📋 Следующие шаги:

### 1. Первый запуск Android Studio (сейчас открыт)

При первом запуске Android Studio:

1. **Выберите установку:**
   - Выберите **"Standard"** (стандартная установка)
   - Нажмите **"Next"**

2. **Дождитесь загрузки:**
   - Android Studio автоматически скачает и установит:
     - Android SDK
     - Android SDK Platform-Tools
     - Android Emulator
   - Это займет несколько минут (зависит от скорости интернета)

3. **Завершите настройку:**
   - Нажмите **"Finish"** когда установка завершится
   - Android Studio откроется

### 2. Установка дополнительных компонентов SDK

После завершения первого запуска, выполните в терминале:

```bash
cd mobile
./setup-android-sdk.sh
```

Этот скрипт установит:
- Android Platform API 33 и 34
- Build Tools
- System Images для эмулятора
- Все необходимые компоненты

### 3. Создание эмулятора

1. **В Android Studio:**
   - Перейдите: `Tools` → `Device Manager`
   - Или нажмите на иконку телефона в правой панели

2. **Создайте устройство:**
   - Нажмите **"Create Device"**
   - Выберите устройство (рекомендуется **Pixel 5** или **Pixel 6**)
   - Нажмите **"Next"**

3. **Выберите системный образ:**
   - Выберите **Android 13 (Tiramisu)** или **Android 14 (UpsideDownCake)**
   - Если образа нет, нажмите **"Download"** рядом с нужным
   - После загрузки нажмите **"Next"**

4. **Завершите создание:**
   - Оставьте настройки по умолчанию
   - Нажмите **"Finish"**

### 4. Запуск приложения

После создания эмулятора, запустите приложение:

```bash
cd mobile
./start-android.sh
```

Или вручную:

```bash
# 1. Запустите эмулятор через Android Studio (Device Manager → ▶️)
# Или через командную строку:
emulator -avd <имя_эмулятора>

# 2. В другом терминале:
cd mobile
npm run android
```

## 🔧 Проверка установки

Проверьте, что все установлено правильно:

```bash
# Примените переменные окружения
source ~/.zshrc

# Проверьте adb
adb version

# Проверьте эмулятор
emulator -version

# Список доступных эмуляторов
emulator -list-avds

# Список подключенных устройств
adb devices
```

## ❓ Решение проблем

### Android Studio не открывается
```bash
# Попробуйте открыть напрямую:
open "/Applications/Android Studio.app"
```

### SDK не найден после установки
- Убедитесь, что завершили первый запуск Android Studio
- Проверьте путь: `ls -la ~/Library/Android/sdk`
- Примените переменные: `source ~/.zshrc`

### Эмулятор не запускается
- Убедитесь, что системный образ установлен
- Проверьте, что эмулятор создан: `emulator -list-avds`
- Для Apple Silicon Mac: используйте ARM64 образы

### Expo не видит эмулятор
- Убедитесь, что эмулятор полностью загрузился
- Проверьте: `adb devices` должен показывать устройство
- Перезапустите adb: `adb kill-server && adb start-server`

## 📚 Дополнительная информация

Подробная инструкция: [ANDROID_EMULATOR_SETUP.md](./ANDROID_EMULATOR_SETUP.md)



