# 🚀 Локальный запуск мобильной версии

## ✅ Что было сделано:

- ✅ Удален tunnel режим
- ✅ Настроен только локальный запуск
- ✅ Создан скрипт для запуска сервисов

## 🚀 Быстрый запуск:

### Способ 1: Автоматический запуск всех сервисов

```bash
cd mobile
./start-services.sh
```

Этот скрипт автоматически:
- Проверит и запустит backend на порту 8000
- Запустит Expo на порту 8081
- Покажет IP адрес для подключения

### Способ 2: Ручной запуск

#### 1. Запустите backend (если не запущен):

```bash
cd backend
source venv/bin/activate
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

#### 2. Запустите Expo:

```bash
cd mobile
npm run android
```

Или используйте скрипт:
```bash
cd mobile
./start.sh
```

## 📱 Подключение с эмулятора:

### Для Android эмулятора:

1. **Узнайте ваш IP адрес:**
   ```bash
   ifconfig | grep "inet " | grep -v 127.0.0.1
   ```

2. **В Expo Go на эмуляторе:**
   - Откройте Expo Go
   - Введите URL: `exp://ВАШ_IP:8081`
   - Например: `exp://192.168.0.128:8081`

3. **Или используйте QR код** из терминала Expo

## ⚙️ Настройка API URL:

Для Android эмулятора API URL уже настроен: `http://10.0.2.2:8000`

Для реального устройства обновите `src/services/api.js`:
```javascript
const API_BASE_URL = 'http://ВАШ_IP:8000'
```

## 📋 Проверка сервисов:

```bash
# Проверить backend
lsof -ti:8000 && echo "Backend запущен" || echo "Backend не запущен"

# Проверить Expo
lsof -ti:8081 && echo "Expo запущен" || echo "Expo не запущен"
```

## 🛑 Остановка сервисов:

```bash
# Остановить backend
lsof -ti:8000 | xargs kill -9

# Остановить Expo
lsof -ti:8081 | xargs kill -9
```

## 📝 Важно:

- Backend должен быть доступен на `0.0.0.0:8000` (не только localhost)
- Убедитесь что эмулятор и компьютер в одной сети (для реального устройства)
- Для Android эмулятора используется специальный IP `10.0.2.2` для доступа к хосту


