# 📱 Настройка для реального Android телефона

## ✅ Готово к использованию!

API настроен для работы с реальными Android устройствами по умолчанию.

## 🚀 Подключение:

### 1. Запустите сервисы:

```bash
cd mobile
./start-services.sh
```

Этот скрипт запустит:
- ✅ Backend на `http://0.0.0.0:8000` (доступен из сети)
- ✅ Expo на порту 8081
- ✅ Покажет IP адрес для подключения

### 2. Подключитесь с телефона:

1. **Установите Expo Go** на Android:
   - Google Play: https://play.google.com/store/apps/details?id=host.exp.exponent

2. **Убедитесь что телефон в той же Wi-Fi сети что и компьютер**

3. **Откройте Expo Go** на телефоне

4. **Подключитесь:**
   - Нажмите "Enter URL manually"
   - Введите: `exp://192.168.11.153:8081`
   - Нажмите "Connect"

   Или **отсканируйте QR код** из терминала Expo

## 📋 Текущие настройки:

- **IP компьютера:** `192.168.11.153`
- **Expo URL:** `exp://192.168.11.153:8081`
- **API URL:** `http://192.168.11.153:8000`

## 🔄 Если IP изменился:

Обновите в `src/services/api.js`:

```javascript
const HOST_IP = 'НОВЫЙ_IP'
```

Узнать IP:
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1 | head -1 | awk '{print $2}'
```

## 🔧 Переключение на эмулятор:

Если нужно использовать эмулятор, измените в `src/services/api.js`:

```javascript
return 'http://10.0.2.2:8000'  // для эмулятора
```

Вместо:
```javascript
return `http://${HOST_IP}:8000`  // для реального устройства
```

## ✅ Что проверено:

- ✅ API автоматически использует правильный URL для реальных устройств
- ✅ Backend запущен с `--host 0.0.0.0` (доступен из сети)
- ✅ Expo настроен для локального режима
- ✅ Нет ошибок линтера

Подключайтесь с телефона! 📱


