# 📱 Подключение с реального Android телефона

## ✅ Настройка завершена!

API автоматически настроен для работы с реальными устройствами.

## 🚀 Быстрый старт:

### 1. Запустите сервисы:

```bash
cd mobile
./start-services.sh
```

Или вручную:
```bash
# Backend (в одном терминале)
cd backend
source venv/bin/activate
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# Expo (в другом терминале)
cd mobile
npm run android
```

### 2. Подключитесь с телефона:

1. **Установите Expo Go** на Android телефон:
   - Откройте [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)
   - Установите "Expo Go"

2. **Убедитесь что телефон и компьютер в одной Wi-Fi сети**

3. **Откройте Expo Go** на телефоне

4. **Подключитесь:**
   - Нажмите "Enter URL manually"
   - Введите: `exp://192.168.11.153:8081`
   - Нажмите "Connect"

   Или отсканируйте **QR код** из терминала где запущен Expo

### 3. Готово!

Приложение должно загрузиться на телефоне и подключиться к backend.

## 🔧 Текущие настройки:

- **Ваш IP адрес:** `192.168.11.153`
- **Expo URL:** `exp://192.168.11.153:8081`
- **API URL (реальное устройство):** `http://192.168.11.153:8000`
- **API URL (эмулятор):** `http://10.0.2.2:8000`

## 🔄 Обновление IP адреса:

Если IP изменился, обновите в `src/services/api.js`:

```javascript
const HOST_IP = 'НОВЫЙ_IP'
```

Узнать текущий IP:
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1 | head -1 | awk '{print $2}'
```

## ⚠️ Решение проблем:

### Телефон не может подключиться к Expo:

1. **Проверьте Wi-Fi:** телефон и компьютер должны быть в одной сети
2. **Проверьте файрвол macOS:** может блокировать порт 8081
   - Системные настройки → Защита и безопасность → Файрвол
   - Временно отключите или добавьте разрешение для Node.js
3. **Проверьте Expo:** должен быть запущен на порту 8081

### Backend недоступен:

1. **Проверьте что backend запущен:**
   ```bash
   lsof -ti:8000 && echo "✅ Backend работает"
   ```

2. **Проверьте доступность с телефона:**
   - Откройте в браузере телефона: `http://192.168.11.153:8000/docs`
   - Должна открыться документация API

3. **Убедитесь что backend запущен с `--host 0.0.0.0`**

### API запросы не работают:

1. Проверьте что используется правильный IP в `src/services/api.js`
2. Для реальных устройств должен быть: `http://192.168.11.153:8000`
3. Проверьте логи backend - должны приходить запросы

## 📋 Проверка подключения:

```bash
# Проверить backend
lsof -ti:8000 && echo "✅ Backend работает"

# Проверить Expo
lsof -ti:8081 && echo "✅ Expo работает"

# Узнать IP
ifconfig | grep "inet " | grep -v 127.0.0.1
```


