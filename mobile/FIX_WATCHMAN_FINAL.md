# 🔧 Окончательное решение проблемы watchman

## Проблема:
Watchman не может получить доступ к директории проекта из-за прав доступа.

## ✅ Решение: Запустить без watchman

Expo может работать без watchman, используя альтернативный файловый watcher.

### Способ 1: Использовать скрипт (рекомендуется)

```bash
cd mobile
./START_WITHOUT_WATCHMAN.sh
```

### Способ 2: Установить переменную окружения

```bash
cd mobile
export EXPO_NO_WATCHMAN=1
npx expo start --tunnel --android
```

### Способ 3: Использовать флаг (если поддерживается)

```bash
cd mobile
npx expo start --tunnel --android --no-watchman
```

## 📝 Что было сделано:

1. ✅ Создан файл `.watchmanconfig` в корне проекта
2. ✅ Создан скрипт `START_WITHOUT_WATCHMAN.sh` для запуска без watchman

## ⚠️ Важно:

При запуске Expo может спросить об установке @expo/ngrok:
```
? The package @expo/ngrok@^4.1.0 is required to use tunnels, would you like to install it globally? › (Y/n)
```

**Ответьте: Y**

## 🚀 После запуска:

В терминале появится tunnel URL типа:
```
› Metro waiting on exp://abc-def-ghi.tunnel.exp.direct:80
```

Это ваш реальный Expo URL!



