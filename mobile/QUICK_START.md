# 🚀 Быстрый запуск

## ✅ Простой способ (рекомендуется):

```bash
cd mobile
./start.sh
```

Или:

```bash
cd mobile
npm run android
```

## 📋 Что происходит:

- ✅ Запускается Expo **без watchman** (избегает проблем с правами)
- ✅ Используется **локальный режим** (LAN)
- ✅ Автоматически открывается на Android эмуляторе

## 📱 После запуска:

1. **В терминале появится QR код** и URL для подключения
2. **На эмуляторе откройте Expo Go**
3. **Введите URL** из терминала (например: `exp://192.168.11.153:8081`)
4. Или **отсканируйте QR код** из терминала

## 🔄 Доступные команды:

Все команды запускаются **без watchman**:

- `npm start` - запуск Expo
- `npm run android` - запуск для Android (локальный режим)
- `npm run ios` - запуск для iOS
- `npm run web` - запуск в браузере

## 💡 Альтернатива - запуск всех сервисов:

Для автоматического запуска backend и Expo:

```bash
cd mobile
./start-services.sh
```

Этот скрипт запустит:
- Backend на порту 8000
- Expo на порту 8081
- Покажет IP адрес для подключения

## ⚙️ Настройка API URL:

Для Android эмулятора уже настроено: `http://10.0.2.2:8000`

Для реального устройства обновите `src/services/api.js`:
```javascript
const API_BASE_URL = 'http://ВАШ_IP:8000'
```

## 📝 Важно:

- Backend должен быть запущен на порту 8000
- Для Android эмулятора используется специальный IP `10.0.2.2` для доступа к хосту
- Убедитесь что эмулятор и компьютер в одной сети (для реального устройства)
