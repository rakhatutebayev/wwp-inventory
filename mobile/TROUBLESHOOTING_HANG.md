# 🔧 Решение проблемы зависания приложения

## Если приложение зависло:

### 1. Перезапустите Expo с очисткой кэша:

```bash
cd mobile
lsof -ti:8081 | xargs kill -9  # Остановить старый Expo
rm -rf .expo node_modules/.cache  # Очистить кэш
npm run android
```

### 2. Проверьте подключение:

#### Проверка Expo:
```bash
curl http://localhost:8081/status
```

#### Проверка Backend:
```bash
curl http://192.168.11.153:8000/health
```

Должен вернуться: `{"status":"ok"}`

### 3. Перезагрузите приложение в Expo Go:

На телефоне в Expo Go:
- Нажмите кнопку **"Reload"** (обновить)
- Или встряхните телефон и выберите "Reload"
- Или закройте и откройте Expo Go заново

### 4. Проверьте что телефон в правильной сети:

- Телефон и компьютер должны быть в **одной Wi-Fi сети**
- Проверьте на телефоне: Настройки → Wi-Fi → Убедитесь что подключены к той же сети

### 5. Проверьте доступность backend с телефона:

На телефоне откройте браузер и перейдите:
```
http://192.168.11.153:8000/docs
```

Должна открыться документация API. Если не открывается - проблема с сетью или файрволом.

### 6. Проверьте файрвол macOS:

Файрвол может блокировать подключения:

1. Системные настройки → Защита и безопасность → Файрвол
2. Проверьте настройки файрвола
3. Временно отключите для проверки

### 7. Полный перезапуск:

```bash
# Остановить все
lsof -ti:8000 | xargs kill -9  # Backend
lsof -ti:8081 | xargs kill -9  # Expo

# Запустить заново
cd backend
source venv/bin/activate
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload &

cd ../mobile
npm run android
```

### 8. Проверьте логи:

Если приложение все еще зависает, проверьте логи:

```bash
# Логи Expo в терминале
# Логи backend в терминале где запущен uvicorn
```

## 💡 Быстрое решение:

1. **Перезагрузите Expo Go** на телефоне (закройте и откройте)
2. **Перезапустите Expo** с очисткой кэша
3. **Проверьте подключение к backend** с телефона через браузер

## ✅ Если ничего не помогает:

Попробуйте подключиться заново:
1. Закройте Expo Go на телефоне
2. Остановите Expo: `lsof -ti:8081 | xargs kill -9`
3. Запустите заново: `cd mobile && npm run android`
4. Подключитесь заново с URL: `exp://192.168.11.153:8081`


