# 📱 Ваш Expo URL адрес

## 🌐 Текущий LAN URL:

```
exp://192.168.0.128:8081
```

**⚠️ Проблема:** macOS блокирует доступ к этому порту с внешних устройств.

## ✅ Решение: Tunnel URL (рекомендуется)

Tunnel URL обходит блокировку macOS и работает через облако Expo.

### Как получить tunnel URL:

#### Вариант 1: Запустить Expo и скопировать из терминала

```bash
cd mobile
npx expo start --tunnel --android
```

В терминале появится что-то вроде:
```
› Metro waiting on exp://abc-def-ghi-123.tunnel.exp.direct:80
```

**Это и есть ваш реальный tunnel URL!**

#### Вариант 2: Использовать скрипт

```bash
cd mobile
./get-url.sh
```

Скрипт автоматически получит tunnel URL (может занять 30-60 секунд).

#### Вариант 3: Проверить через curl

После запуска Expo в tunnel режиме:

```bash
curl -s http://localhost:8081/ | python3 -c "import sys, json; data=json.load(sys.stdin); print('exp://' + data['expoGo']['debuggerHost'])"
```

## 📋 Использование URL:

1. **Откройте Expo Go** на эмуляторе/телефоне
2. **Введите URL** (tunnel или LAN)
3. Или используйте **QR код** из терминала/`generate-qr.html`

## 🔄 Обновление URL в HTML генераторе:

Если получили новый tunnel URL, обновите `generate-qr.html`:

1. Откройте `generate-qr.html` в браузере
2. Вставьте новый URL в поле
3. Нажмите "Сгенерировать QR код"

## 💡 Рекомендация:

**Используйте tunnel URL** - он обходит блокировку macOS и работает стабильнее.

Текущий tunnel URL будет показан в терминале когда запустите:
```bash
npx expo start --tunnel --android
```



