# ✅ Поддержка форматов этикеток и авто-подгонка размеров

## 🎯 Что было реализовано:

### 1. **Добавлены новые форматы:**
   - ✅ **40x30 мм** - для SUPVAN T50M Pro
   - ✅ **50x40 мм** - для SUPVAN T50M Pro
   - ✅ Поддержка существующих форматов: 38x21, 50x25, 70x36, 100x50 мм

### 2. **Автоматическая подгонка размеров (Auto-Fit):**
   - ✅ Умный алгоритм расчета размеров QR кода на основе формата этикетки
   - ✅ Автоматический расчет размера шрифта с учетом:
     - Размеров этикетки
     - Длины текста (модель, серийный номер, инвентарный номер)
     - Доступного пространства
   - ✅ Адаптивные размеры для разных элементов:
     - Модель (заголовок) - самый крупный шрифт
     - Серийный номер - средний шрифт
     - Инвентарный номер - крупный шрифт (важная информация)
   - ✅ Динамическое изменение размера QR кода в зависимости от длины текста

### 3. **Диалог выбора формата:**
   - ✅ При нажатии на кнопку "Печать" открывается диалог с выбором формата
   - ✅ Отображаются все доступные форматы с размерами
   - ✅ Выделение выбранного формата
   - ✅ Автоматический запуск печати после выбора формата

### 4. **Поддержка принтеров:**
   - ✅ **Phomemo M110** - через системный диалог печати Android/iOS
   - ✅ **SUPVAN T50M Pro** - через системный диалог печати Android/iOS
   - ✅ Все Bluetooth принтеры, совместимые с системным API печати

## 📐 Алгоритм авто-подгонки:

### Расчет размеров:

1. **Определение доступного пространства:**
   ```javascript
   availableWidth = width - padding * 2
   availableHeight = height - padding * 2
   ```

2. **Расчет размера QR кода:**
   - Базовый размер: 60% от доступной высоты или 70% от ширины (минимум)
   - Если текст длинный (>20 символов), QR код уменьшается до 60% ширины

3. **Расчет размера шрифта:**
   - Базовый размер вычисляется на основе:
     - Доступной ширины / длина текста
     - Доступной высоты / количество строк
   - Ограничение: от 6pt до 16pt
   - Разные размеры для разных элементов:
     - Заголовок (модель): базовый * 1.2 (макс 14pt)
     - Серийный номер: базовый * 0.9 (мин 6pt)
     - Инвентарный номер: базовый размер (мин 7pt)

### Форматы этикеток:

```javascript
LABEL_FORMATS = {
  "38x21": { width: 38, height: 21 },
  "40x30": { width: 40, height: 30 }, // SUPVAN T50M Pro
  "50x25": { width: 50, height: 25 },
  "50x40": { width: 50, height: 40 }, // SUPVAN T50M Pro
  "70x36": { width: 70, height: 36 },
  "100x50": { width: 100, height: 50 },
}
```

## 🔧 Технические детали:

### printService.js:
- `calculateAutoFitSizes()` - функция расчета оптимальных размеров
- `generateLabelHTML()` - генерация HTML с авто-подгонкой
- `LABEL_FORMATS` - экспортируемый список форматов
- Использование CSS для правильного отображения:
  - `flexbox` для распределения пространства
  - `word-wrap` и `overflow-wrap` для длинного текста
  - `object-fit: contain` для QR кода

### DeviceDetailScreen.js:
- Диалог выбора формата с использованием `Portal` и `Dialog`
- Сохранение выбранного формата в state
- Автоматический запуск печати после выбора

## ✅ Преимущества:

1. **Универсальность:** Работает с любыми размерами этикеток
2. **Автоматизация:** Не нужно вручную подбирать размеры шрифтов
3. **Читаемость:** Все данные всегда помещаются на этикетку
4. **Гибкость:** Легко добавить новые форматы
5. **Поддержка длинного текста:** Автоматическое уменьшение размеров при необходимости

## 📱 Использование:

1. Пользователь нажимает "Печать"
2. Открывается диалог выбора формата
3. Пользователь выбирает формат (например, 40x30 мм для SUPVAN T50M Pro)
4. Система автоматически рассчитывает оптимальные размеры
5. Открывается системный диалог выбора принтера
6. Пользователь выбирает принтер (SUPVAN T50M Pro, Phomemo M110 и т.д.)
7. Печать отправляется с правильными размерами

Теперь система поддерживает все популярные форматы этикеток и автоматически подгоняет размеры! 🎯


