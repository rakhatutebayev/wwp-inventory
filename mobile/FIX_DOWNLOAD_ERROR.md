# 🔧 Исправление ошибки "Failed to download remote update"

## Проблема:
Expo Go не может загрузить бандл с сервера разработки.

## ✅ Решения:

### Вариант 1: Использовать tunnel режим (рекомендуется)

Tunnel режим создает стабильное соединение через облако Expo:

```bash
cd mobile
lsof -ti:8081 | xargs kill -9  # Остановить старый Expo
rm -rf .expo node_modules/.cache  # Очистить кэш
npx expo start --tunnel --android
```

В терминале появится URL типа: `exp://xxx.tunnel.exp.direct:80`

Используйте этот URL в Expo Go.

### Вариант 2: Проверить сеть и перезапустить

```bash
# 1. Остановить Expo
lsof -ti:8081 | xargs kill -9

# 2. Очистить кэш
cd mobile
rm -rf .expo node_modules/.cache

# 3. Проверить IP адрес
ifconfig | grep "inet " | grep -v 127.0.0.1

# 4. Запустить с LAN режимом
npx expo start --android --host lan
```

### Вариант 3: Проверить доступность сервера

Убедитесь, что Expo сервер доступен:
```bash
curl http://localhost:8081/status
```

Должен вернуть JSON с информацией о проекте.

### Вариант 4: Использовать localhost напрямую

Попробуйте использовать localhost (если эмулятор поддерживает):
```bash
npx expo start --android --localhost
```

Затем в Expo Go используйте: `exp://localhost:8081`

## 🔍 Диагностика:

1. **Проверьте терминал Expo** - есть ли ошибки компиляции?
2. **Проверьте подключение**: `adb devices`
3. **Проверьте порт**: `lsof -ti:8081`
4. **Проверьте логи**: `adb logcat | grep -i expo`

## 💡 Рекомендация:

**Используйте tunnel режим** - он наиболее стабильный и не зависит от локальной сети.



