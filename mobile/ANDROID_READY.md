# ✅ Android эмулятор настроен и готов к работе!

## 🎉 Что установлено и настроено:

1. ✅ **Android Studio** - установлен
2. ✅ **Android SDK** - настроен в `~/Library/Android/sdk`
3. ✅ **Command-line Tools** - установлены
4. ✅ **Java 17** - настроена для работы с Android SDK
5. ✅ **Эмулятор создан**: `Medium_Phone_API_36.1`
6. ✅ **Переменные окружения** настроены в `~/.zshrc`:
   - `ANDROID_HOME`
   - `JAVA_HOME`
   - Пути к инструментам Android

## 🚀 Как запустить приложение:

### Вариант 1: Автоматический запуск (рекомендуется)

```bash
cd mobile
./start-android.sh
```

Этот скрипт:
- Проверит наличие эмулятора
- Запустит эмулятор если он не запущен
- Дождется загрузки эмулятора
- Запустит Expo приложение

### Вариант 2: Ручной запуск

```bash
# 1. Запустите эмулятор (если не запущен)
source ~/.zshrc
emulator -avd Medium_Phone_API_36.1 &

# 2. Дождитесь загрузки (проверьте: adb devices)

# 3. Запустите приложение
cd mobile
npm run android
```

## 📱 Текущий эмулятор:

- **Имя**: `Medium_Phone_API_36.1`
- **API Level**: 36 (Android 15)
- **Статус**: Готов к использованию

## 🔧 Полезные команды:

### Проверка подключенных устройств:
```bash
source ~/.zshrc
adb devices
```

### Список всех эмуляторов:
```bash
emulator -list-avds
```

### Запуск конкретного эмулятора:
```bash
emulator -avd Medium_Phone_API_36.1
```

### Остановка эмулятора:
```bash
adb -s emulator-5554 emu kill
```

### Просмотр логов:
```bash
adb logcat
```

### Перезапуск adb:
```bash
adb kill-server
adb start-server
```

## 📝 Важные заметки:

1. **Применение переменных окружения:**
   После открытия нового терминала выполните:
   ```bash
   source ~/.zshrc
   ```
   Или перезапустите терминал.

2. **Порт Expo:**
   Если порт 8081 занят, Expo автоматически предложит использовать другой порт.

3. **Backend должен быть запущен:**
   Убедитесь, что backend сервер запущен на порту 8000:
   ```bash
   cd backend
   uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
   ```

4. **Настройка API URL:**
   Если нужно изменить URL API для подключения к backend, отредактируйте:
   `mobile/src/services/api.js`

## ❓ Решение проблем:

### Эмулятор не запускается
- Проверьте, что эмулятор создан: `emulator -list-avds`
- Убедитесь, что системный образ установлен
- Попробуйте запустить через Android Studio: Tools → Device Manager

### Expo не видит эмулятор
- Убедитесь, что эмулятор полностью загрузился: `adb devices` должен показывать `device`
- Перезапустите adb: `adb kill-server && adb start-server`
- Перезапустите Expo: нажмите `r` в терминале Expo или перезапустите `npm run android`

### Ошибки Java
- Убедитесь, что Java 17 используется: `java -version`
- Примените переменные: `source ~/.zshrc`

### Порт занят
- Остановите старый процесс: `lsof -ti:8081 | xargs kill -9`
- Или используйте другой порт: `npx expo start --android --port 8082`

## 🎯 Следующие шаги:

1. Убедитесь, что backend запущен
2. Запустите эмулятор и приложение
3. Протестируйте функциональность приложения
4. При необходимости создайте дополнительные эмуляторы через Android Studio

---

**Готово к работе!** 🚀



