# 📱 Запуск без Expo Go

## Что такое Expo Go?

**Expo Go** - это приложение для разработки, которое позволяет быстро тестировать приложения без сборки. Оно нужно только для разработки.

## ✅ Можно ли без Expo Go?

**Да!** Можно собрать standalone APK и установить его напрямую на эмулятор, как обычное приложение.

## 🚀 Быстрый способ (рекомендуется для тестирования)

### Вариант 1: Использовать Expo Go (самый простой)

Expo Go уже установлен на эмуляторе. Просто:
1. Откройте Expo Go на эмуляторе
2. Введите URL: `exp://192.168.11.157:8081`
3. Готово!

**Преимущества:**
- ✅ Мгновенный запуск
- ✅ Не нужно собирать APK
- ✅ Автоматическое обновление при изменениях кода

### Вариант 2: Собрать standalone APK

Если хотите установить приложение как обычное (без Expo Go):

#### Шаг 1: Установите EAS CLI

```bash
cd mobile
npm install -g eas-cli
# Или используйте npx (без установки):
# npx eas-cli
```

#### Шаг 2: Войдите в Expo (или создайте аккаунт)

```bash
npx eas-cli login
```

#### Шаг 3: Соберите APK

**Для быстрого тестирования (development build):**
```bash
cd mobile
npx eas-cli build --platform android --profile development --local
```

**Или через облако (проще, но дольше):**
```bash
cd mobile
npx eas-cli build --platform android --profile development
```

#### Шаг 4: Установите APK на эмулятор

После сборки:
```bash
# Найдите APK файл (обычно в корне проекта или в build/)
adb install *.apk

# Или укажите путь явно
adb install path/to/your-app.apk
```

#### Шаг 5: Запустите приложение

```bash
adb shell monkey -p com.wwp.inventory -c android.intent.category.LAUNCHER 1
```

## 📋 Использование готового скрипта

Я создал скрипт для автоматической сборки и установки:

```bash
cd mobile
./build-and-install.sh
```

Этот скрипт:
1. Проверит эмулятор
2. Соберет APK
3. Установит на эмулятор
4. Запустит приложение

## ⚡ Что выбрать?

### Используйте Expo Go если:
- ✅ Быстрое тестирование
- ✅ Разработка и отладка
- ✅ Частые изменения кода

### Используйте standalone APK если:
- ✅ Финальное тестирование
- ✅ Демонстрация клиенту
- ✅ Распространение приложения
- ✅ Тестирование без интернета

## 🔧 Текущая рекомендация

**Для вашего случая** (тестирование на эмуляторе) **лучше использовать Expo Go**, потому что:
1. Уже установлен
2. Быстрее запускается
3. Не нужно собирать APK
4. Автоматически обновляется при изменениях

**Просто откройте Expo Go на эмуляторе и введите URL:**
```
exp://192.168.11.157:8081
```

## 📝 Настройки уже готовы

Я уже:
- ✅ Установил `expo-dev-client` (для будущих сборок)
- ✅ Настроил `app.json` с package name
- ✅ Создал `eas.json` для сборки
- ✅ Создал скрипт `build-and-install.sh`

Если в будущем захотите собрать APK - все готово!



