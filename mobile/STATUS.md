# 📊 Текущий статус сервисов

## ✅ Запущенные сервисы:

- ✅ **Backend**: http://localhost:8000 (PID: 73135)
- ✅ **Expo**: http://localhost:8081 (PID: 13021)

## 📱 Подключение к приложению:

### Ваш IP адрес: `192.168.11.153`

### Для Android эмулятора:

1. Откройте **Expo Go** на эмуляторе
2. Введите URL: `exp://192.168.11.153:8081`
3. Нажмите "Connect"

### Для реального устройства:

1. Убедитесь, что устройство в той же Wi-Fi сети
2. В Expo Go введите: `exp://192.168.11.153:8081`
3. Обновите API URL в `src/services/api.js` на ваш IP: `http://192.168.11.153:8000`

## 🔧 Проверка статуса:

```bash
# Backend
lsof -ti:8000 && echo "✅ Backend работает" || echo "❌ Backend не запущен"

# Expo
lsof -ti:8081 && echo "✅ Expo работает" || echo "❌ Expo не запущен"
```

## 🛑 Остановка сервисов:

```bash
lsof -ti:8000 | xargs kill -9  # Backend
lsof -ti:8081 | xargs kill -9  # Expo
```


