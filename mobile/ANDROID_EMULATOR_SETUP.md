# Настройка Android эмулятора для WWP Inventory

## 📋 Шаг 1: Установка Android Studio

1. **Скачайте Android Studio:**
   - Перейдите на https://developer.android.com/studio
   - Скачайте версию для macOS
   - Установите приложение

2. **При первом запуске Android Studio:**
   - Выберите "Standard" установку
   - Дождитесь загрузки компонентов SDK

## 📋 Шаг 2: Установка Android SDK и эмулятора

1. **Откройте Android Studio**
2. **Перейдите в Settings (Preferences на macOS):**
   - `Android Studio` → `Settings` (или `Preferences` на macOS)
   - Или `Cmd + ,` (macOS)

3. **Установите SDK:**
   - Перейдите в `Appearance & Behavior` → `System Settings` → `Android SDK`
   - Вкладка `SDK Platforms`:
     - ✅ Установите `Android 13.0 (Tiramisu)` или `Android 14.0`
     - ✅ Установите `Android SDK Platform-Tools`
   - Вкладка `SDK Tools`:
     - ✅ Установите `Android Emulator`
     - ✅ Установите `Android SDK Build-Tools`
     - ✅ Установите `Android SDK Command-line Tools`
   - Нажмите `Apply` и дождитесь установки

## 📋 Шаг 3: Создание Android эмулятора

1. **Откройте AVD Manager:**
   - В Android Studio: `Tools` → `Device Manager`
   - Или нажмите на иконку телефона в правой панели

2. **Создайте новый виртуальный девайс:**
   - Нажмите `Create Device`
   - Выберите устройство (например, `Pixel 5` или `Pixel 6`)
   - Нажмите `Next`

3. **Выберите системный образ:**
   - Выберите `Tiramisu` (API 33) или `UpsideDownCake` (API 34)
   - Если образа нет, нажмите `Download` рядом с нужным
   - Нажмите `Next`

4. **Настройте эмулятор:**
   - Оставьте настройки по умолчанию или измените при необходимости
   - Нажмите `Finish`

## 📋 Шаг 4: Настройка переменных окружения

Добавьте в `~/.zshrc` (или `~/.bash_profile`):

```bash
# Android SDK
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/emulator
export PATH=$PATH:$ANDROID_HOME/platform-tools
export PATH=$PATH:$ANDROID_HOME/tools
export PATH=$PATH:$ANDROID_HOME/tools/bin
```

Примените изменения:
```bash
source ~/.zshrc
```

Проверьте установку:
```bash
adb version
emulator -version
```

## 📋 Шаг 5: Запуск эмулятора

### Вариант 1: Через Android Studio
1. Откройте Android Studio
2. `Tools` → `Device Manager`
3. Нажмите ▶️ рядом с нужным эмулятором

### Вариант 2: Через командную строку
```bash
# Список доступных эмуляторов
emulator -list-avds

# Запуск конкретного эмулятора
emulator -avd <имя_эмулятора>
```

## 📋 Шаг 6: Запуск приложения

1. **Убедитесь, что эмулятор запущен** (дождитесь полной загрузки)

2. **Перейдите в папку mobile:**
   ```bash
   cd mobile
   ```

3. **Установите зависимости (если еще не установлены):**
   ```bash
   npm install
   ```

4. **Запустите Expo с Android:**
   ```bash
   npm run android
   ```
   
   Или:
   ```bash
   npx expo start --android
   ```

5. **Expo автоматически:**
   - Определит запущенный эмулятор
   - Установит Expo Go (если нужно)
   - Запустит приложение

## 🔧 Полезные команды

### Проверка подключенных устройств:
```bash
adb devices
```

### Перезапуск adb сервера (если проблемы):
```bash
adb kill-server
adb start-server
```

### Просмотр логов:
```bash
adb logcat
```

### Очистка кэша Expo:
```bash
npx expo start --clear
```

## ❓ Решение проблем

### Эмулятор не запускается
- Убедитесь, что установлен HAXM (Intel) или Hypervisor Framework (Apple Silicon)
- Для Apple Silicon Mac: эмулятор работает из коробки
- Для Intel Mac: может потребоваться установка HAXM

### Expo не видит эмулятор
- Убедитесь, что эмулятор полностью загрузился
- Проверьте: `adb devices` должен показывать устройство
- Перезапустите adb: `adb kill-server && adb start-server`

### Ошибка "SDK location not found"
- Установите переменную `ANDROID_HOME` (см. Шаг 4)
- Перезапустите терминал

### Медленная работа эмулятора
- Увеличьте RAM для эмулятора в AVD Manager
- Используйте x86/x86_64 образы (быстрее чем ARM)
- Для Apple Silicon: используйте ARM64 образы

## 🚀 Быстрый старт (после настройки)

```bash
# 1. Запустите эмулятор (в отдельном терминале или через Android Studio)
emulator -avd <имя_эмулятора>

# 2. В другом терминале запустите приложение
cd mobile
npm run android
```



