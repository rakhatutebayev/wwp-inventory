# 📦 Сборка APK без Expo Go

## Что такое Expo Go?

**Expo Go** - это приложение для разработки, которое позволяет быстро тестировать приложения без сборки. Но для продакшена или для установки напрямую на эмулятор можно собрать standalone APK.

## ✅ Преимущества standalone APK:

- ✅ Не нужен Expo Go
- ✅ Установка как обычное приложение
- ✅ Работает автономно
- ✅ Можно распространять

## 🚀 Сборка APK для эмулятора

### Вариант 1: Локальная сборка (development build)

```bash
cd mobile

# Установите EAS CLI (если еще не установлен)
npm install -g eas-cli

# Войдите в Expo (или создайте аккаунт)
eas login

# Соберите development build
eas build --platform android --profile development --local
```

### Вариант 2: Быстрая сборка через Expo (рекомендуется для тестирования)

```bash
cd mobile

# Соберите APK напрямую
npx expo build:android -t apk
```

**Примечание:** Эта команда устарела в новых версиях Expo. Используйте EAS Build.

### Вариант 3: Использование EAS Build (современный способ)

1. **Настройте EAS:**
```bash
cd mobile
eas build:configure
```

2. **Соберите APK:**
```bash
# Development build (для тестирования)
eas build --platform android --profile development

# Production build (для релиза)
eas build --platform android --profile production
```

3. **После сборки скачайте APK** и установите на эмулятор:
```bash
# Установка APK на эмулятор
adb install path/to/your-app.apk
```

## 📱 Установка APK на эмулятор

После сборки APK:

```bash
# Установите APK на эмулятор
adb install path/to/your-app.apk

# Или если APK в текущей директории
adb install *.apk
```

## 🔧 Альтернатива: Development Build через Expo Dev Client

Можно создать development build, который работает без Expo Go:

```bash
cd mobile

# Создайте development build
npx expo install expo-dev-client

# Соберите APK с dev client
eas build --platform android --profile development --local
```

## ⚡ Быстрый способ для тестирования

Если нужно быстро протестировать без Expo Go, можно использовать **Expo Development Build**:

1. Установите `expo-dev-client`:
```bash
cd mobile
npx expo install expo-dev-client
```

2. Соберите development build:
```bash
eas build --platform android --profile development --local
```

3. Установите на эмулятор:
```bash
adb install *.apk
```

## 📝 Настройка app.json для сборки

Убедитесь, что в `app.json` указаны все необходимые настройки:

```json
{
  "expo": {
    "android": {
      "package": "com.yourcompany.wwpinventory",
      "versionCode": 1
    }
  }
}
```

## 🎯 Рекомендация

Для **быстрого тестирования на эмуляторе** лучше использовать **Expo Go** (проще и быстрее).

Для **продакшена или финального тестирования** используйте **standalone APK**.



