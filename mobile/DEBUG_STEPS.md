# 🔍 Пошаговая диагностика проблемы

## Шаг 1: Проверьте терминал где запущен Expo

В терминале должно быть:
- ✅ QR код
- ✅ URL: `exp://192.168.11.157:8081` или `exp://...`
- ✅ Сообщение "Metro waiting on..."
- ❌ НЕ должно быть ошибок компиляции (красный текст)

**Если есть ошибки - скопируйте их и отправьте мне!**

## Шаг 2: Проверьте подключение

```bash
# Проверьте, что Expo запущен
lsof -ti:8081

# Проверьте доступность
curl http://localhost:8081/status
```

## Шаг 3: Попробуйте простой тест

Я создал файл `App.test.js` - это упрощенная версия приложения.

Временно переименуйте файлы:
```bash
cd mobile
mv App.js App.original.js
mv App.test.js App.js
```

Затем перезапустите Expo:
```bash
lsof -ti:8081 | xargs kill -9
npx expo start --android --clear
```

Если простое приложение загрузится - проблема в коде основного приложения.
Если не загрузится - проблема в настройке Expo/эмулятора.

## Шаг 4: Проверьте версии зависимостей

```bash
cd mobile
npm list expo react react-native
```

Убедитесь, что версии совместимы с Expo SDK 49.

## Шаг 5: Полная переустановка

Если ничего не помогает:

```bash
cd mobile
rm -rf node_modules .expo
npm install
npx expo start --android --clear
```

## Шаг 6: Альтернатива - используйте веб версию

Для быстрой проверки что код работает:

```bash
cd mobile
npx expo start --web
```

Откроется в браузере - если там работает, значит проблема в настройке Android/Expo Go.

## 📝 Что мне нужно для диагностики:

1. **Вывод терминала где запущен Expo** - есть ли ошибки?
2. **Что показывает экран эмулятора** - та же ошибка или другая?
3. **Результат команды**: `lsof -ti:8081`



