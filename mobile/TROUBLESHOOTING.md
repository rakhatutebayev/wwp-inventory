# 🔧 Решение проблем с запуском приложения на Android эмуляторе

## Проблема: Приложение не видно на эмуляторе

### ✅ Что уже проверено:
- ✅ Эмулятор запущен и подключен
- ✅ Expo Go установлен
- ✅ Expo сервер запущен на порту 8081
- ✅ Backend запущен на порту 8000

### 🔍 Возможные причины и решения:

#### 1. Expo Go показывает домашний экран вместо приложения

**Решение:**
1. Откройте Expo Go на эмуляторе вручную (найдите иконку Expo Go в списке приложений)
2. В Expo Go нажмите "Enter URL manually"
3. Введите: `exp://192.168.11.157:8081`
4. Нажмите "Connect"

#### 2. Проблема с сетью между эмулятором и хостом

**Решение:**
Используйте tunnel режим:
```bash
cd mobile
npx expo start --tunnel --android
```

Или используйте LAN режим:
```bash
cd mobile
EXPO_DEVTOOLS_LISTEN_ADDRESS=0.0.0.0 npx expo start --android --host lan
```

#### 3. Приложение показывает ошибку подключения

**Проверьте:**
- Backend запущен: `lsof -ti:8000`
- API URL правильный в `src/services/api.js`: должен быть `http://10.0.2.2:8000` для эмулятора

#### 4. Полная переустановка

```bash
# Остановите все процессы
lsof -ti:8081 | xargs kill -9
adb shell am force-stop host.exp.exponent

# Очистите кэш
cd mobile
rm -rf node_modules/.cache
npx expo start --clear --android
```

### 📱 Ручной запуск через Expo Go:

1. **Откройте Expo Go на эмуляторе:**
   - Найдите иконку "Expo Go" в списке приложений
   - Откройте приложение

2. **Подключитесь к проекту:**
   - В Expo Go нажмите "Enter URL manually"
   - Введите: `exp://192.168.11.157:8081`
   - Или отсканируйте QR код из терминала где запущен Expo

3. **Если не работает, попробуйте:**
   - Использовать IP адрес из `ifconfig`: `exp://<ваш_ip>:8081`
   - Использовать tunnel: `npx expo start --tunnel --android`

### 🔍 Диагностика:

Запустите скрипт проверки:
```bash
cd mobile
./check-app.sh
```

Проверьте логи:
```bash
adb logcat | grep -i expo
```

### 📞 Дополнительная помощь:

Если ничего не помогает:
1. Перезапустите эмулятор
2. Переустановите Expo Go на эмуляторе
3. Используйте tunnel режим для обхода проблем с сетью



