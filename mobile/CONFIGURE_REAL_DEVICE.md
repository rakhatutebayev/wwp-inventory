# 📱 Настройка для реального Android телефона

## ✅ Что было настроено:

1. ✅ API URL настроен для реальных устройств
2. ✅ Используется IP адрес вашего компьютера: `192.168.11.153`
3. ✅ Автоматическое определение платформы

## 🔧 Текущая конфигурация:

API автоматически определяет платформу:
- **Android эмулятор**: `http://10.0.2.2:8000`
- **Реальное Android устройство**: `http://192.168.11.153:8000`
- **iOS симулятор**: `http://localhost:8000`

## 🚀 Подключение с реального телефона:

### Шаг 1: Убедитесь что backend запущен

```bash
# Проверить статус
lsof -ti:8000 && echo "✅ Backend работает" || echo "❌ Backend не запущен"

# Запустить если нужно
cd backend
source venv/bin/activate
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

**Важно:** Backend должен быть запущен с `--host 0.0.0.0` чтобы быть доступным из сети.

### Шаг 2: Убедитесь что телефон в той же Wi-Fi сети

Телефон и компьютер должны быть подключены к одной Wi-Fi сети.

### Шаг 3: Запустите Expo

```bash
cd mobile
npm run android
```

Или используйте скрипт:
```bash
cd mobile
./start.sh
```

### Шаг 4: Подключитесь с телефона

1. **Установите Expo Go** на телефон (если еще не установлен)
   - Android: [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)

2. **Откройте Expo Go** на телефоне

3. **В Expo Go:**
   - Нажмите "Enter URL manually" (или "Ввести URL вручную")
   - Введите URL: `exp://192.168.11.153:8081`
   - Нажмите "Connect"

4. **Или отсканируйте QR код** из терминала где запущен Expo

## 🔄 Обновление IP адреса:

Если IP адрес вашего компьютера изменился, обновите в `src/services/api.js`:

```javascript
const HOST_IP = 'ВАШ_НОВЫЙ_IP'
```

Узнать текущий IP:
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1 | head -1 | awk '{print $2}'
```

## 🔍 Принудительное использование эмулятора:

Если нужно временно использовать эмулятор, можно установить переменную окружения:

В файле `src/services/api.js` измените проверку или временно установите:
```javascript
const isEmulator = true  // принудительно использовать эмулятор
```

## ⚠️ Проблемы с подключением:

### Телефон не может подключиться к Expo:

1. Убедитесь что телефон и компьютер в одной Wi-Fi сети
2. Проверьте файрвол macOS - может блокировать порт 8081
3. Попробуйте отключить файрвол временно или добавить разрешение

### Backend недоступен:

1. Проверьте что backend запущен с `--host 0.0.0.0`
2. Проверьте доступность: откройте в браузере телефона `http://192.168.11.153:8000/docs`
3. Убедитесь что порт 8000 не блокируется файрволом

## 📝 Текущий IP адрес:

**Ваш IP:** `192.168.11.153`

**URL для Expo Go:** `exp://192.168.11.153:8081`

**API URL (для реального устройства):** `http://192.168.11.153:8000`


