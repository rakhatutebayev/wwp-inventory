# ✅ Ответы на вопросы Expo

## Когда Expo спрашивает:

### Установить @expo/ngrok?

**Ответ: Y (Yes)**

Это необходимо для работы tunnel режима. Нажмите `Y` и Enter.

## 🚀 Что делать:

1. **В терминале где запущен Expo**, когда увидите вопрос:
   ```
   ? The package @expo/ngrok@^4.1.0 is required to use tunnels, would you like to install it globally? › (Y/n)
   ```

2. **Нажмите `Y` и Enter**

3. **Подождите установки** (несколько секунд)

4. **Expo продолжит запуск** и покажет tunnel URL

## 📋 После установки:

Вы увидите в терминале:
```
› Metro waiting on exp://xxx-yyy-zzz.tunnel.exp.direct:80
```

Это и есть ваш реальный Expo URL - скопируйте его!

## ⚠️ Если установка не удалась:

Установлен локально в проект. Перезапустите Expo:
```bash
cd mobile
npx expo start --tunnel --android
```



