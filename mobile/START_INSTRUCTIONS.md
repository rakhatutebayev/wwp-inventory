# Инструкция по запуску мобильного приложения

## ✅ Что было исправлено

1. ✅ Исправлены несовместимые версии зависимостей
2. ✅ Установлен watchman для эффективного отслеживания файлов
3. ✅ Приложение запущено с очисткой кэша

## 📱 Что делать сейчас

### 1. Проверьте терминал

В терминале должно появиться:
- QR-код для сканирования
- Меню Expo с опциями
- URL для подключения (например, `exp://192.168.x.x:8081`)

### 2. Если видите QR-код:

**На реальном устройстве:**
1. Установите **Expo Go** на телефон
2. Откройте Expo Go
3. Отсканируйте QR-код
4. Дождитесь загрузки приложения

**На эмуляторе:**
- Нажмите `a` для Android
- Нажмите `i` для iOS (только macOS)
- Нажмите `w` для веб-браузера

### 3. Если приложение не запустилось:

Попробуйте запустить вручную:
```bash
cd mobile
npx expo start --clear
```

Или с конкретной платформой:
```bash
npx expo start --android  # для Android
npx expo start --ios      # для iOS
npx expo start --web      # для браузера
```

## ⚙️ Настройка для тестирования

### Backend должен быть запущен:

```bash
cd backend
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### Для реального устройства настройте API URL:

1. Узнайте IP вашего компьютера:
   ```bash
   ifconfig | grep "inet " | grep -v 127.0.0.1
   ```

2. Откройте `mobile/src/services/api.js`

3. Замените `localhost` на ваш IP:
   ```javascript
   const API_BASE_URL = 'http://192.168.1.XXX:8000'
   ```

4. Перезагрузите приложение (нажмите `r` в терминале Expo)

## 🔧 Полезные команды

- `r` - перезагрузить приложение
- `m` - показать/скрыть меню
- `?` - показать помощь
- `Ctrl+C` - остановить сервер

## ❓ Решение проблем

### Ошибка "too many open files"
- ✅ Решено установкой watchman

### Ошибка зависимостей
- ✅ Исправлено командой `npx expo install --fix`

### Приложение не подключается
- Проверьте, что устройство и компьютер в одной Wi-Fi сети
- Проверьте настройки файрвола
- Попробуйте использовать туннель (нажмите `s` в меню Expo)



