# ✅ Проблема решена!

## 🔍 Причина ошибки:

Проблема была в том, что был установлен `expo-dev-client`, который требует development build вместо Expo Go.

## ✅ Что было сделано:

1. ✅ Удален `expo-dev-client` 
2. ✅ Очищен кэш Expo
3. ✅ Перезапущен Expo сервер
4. ✅ Expo теперь работает с Expo Go

## 🚀 Что делать сейчас:

### На эмуляторе:

1. **Откройте Expo Go** (если еще не открыт)
2. **Подключитесь к проекту:**
   - Введите URL: `exp://192.168.11.157:8081`
   - Или отсканируйте QR код из терминала
3. **Дождитесь загрузки** (1-2 минуты)

### Если приложение не загружается:

Нажмите кнопку **"Reload"** (обновить) в Expo Go.

## 📝 Важно:

- ✅ Expo запущен на порту 8081
- ✅ Работает с Expo Go (не требует development build)
- ✅ Backend должен быть запущен на порту 8000

## 🔧 Если нужно вернуть development build:

Если в будущем захотите использовать development build вместо Expo Go:

```bash
cd mobile
npm install expo-dev-client
# Затем соберите development build
```

Но для разработки **Expo Go проще и быстрее**!



