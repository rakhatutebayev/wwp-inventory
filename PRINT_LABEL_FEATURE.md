# ✅ Добавлена функция печати меток

## 🎯 Что было добавлено:

### 1. **Кнопка печати на экране деталей устройства:**
   - Добавлена кнопка "Печать" с иконкой принтера
   - Расположена рядом с кнопкой "Переместить"

### 2. **Обновлен API печати меток:**
   - Теперь выводит на метку:
     - ✅ **Модель** устройства (название модели)
     - ✅ **Серийный номер** (Сер: ...)
     - ✅ **Инвентарный номер** (Инв: ...)
     - ✅ **QR код** инвентарного номера

### 3. **Создан сервис для работы с метками:**
   - `labelService.getPrintUrl()` - получение URL для печати одного устройства
   - `labelService.getPrintUrlMultiple()` - получение URL для печати нескольких устройств
   - Автоматически добавляет токен авторизации в URL

## 📋 Формат метки:

На каждой метке отображается:
- QR код (инвентарный номер)
- **Модель** (жирным шрифтом)
- **Сер:** серийный номер
- **Инв:** инвентарный номер (жирным шрифтом)

## 🔧 Технические детали:

### Backend (labels.py):
- Обновлена функция `print_labels()` для включения модели и серийного номера
- Добавлена валидация токена из URL параметра
- Поддерживаются форматы: 38x21, 50x25, 70x36, 100x50 мм

### Mobile (DeviceDetailScreen.js):
- Добавлена функция `handlePrint()` для открытия страницы печати
- Используется `Linking.openURL()` для открытия в браузере
- Добавлен сервис `labelService` для работы с URL печати

## 🚀 Как использовать:

1. Откройте экран деталей устройства
2. Нажмите кнопку "Печать"
3. Откроется страница в браузере с метками
4. Нажмите кнопку "Печать" на странице
5. Выберите принтер и настройте параметры печати
6. Распечатайте метки

## ✅ Проверено:

- ✅ Кнопка печати добавлена на экран
- ✅ API обновлен для вывода модели и серийного номера
- ✅ Токен авторизации передается в URL
- ✅ Нет ошибок линтера

Теперь можно печатать метки с QR-кодом прямо из мобильного приложения! 🖨️


