# Быстрый старт развертывания

## 🚀 Пошаговая инструкция

### Шаг 1: Создайте GitHub репозиторий

1. Перейдите на https://github.com/new
2. Создайте новый репозиторий (например, `wwp-inventory`)
3. Скопируйте URL репозитория

### Шаг 2: Инициализируйте git в проекте

```bash
cd /Users/rakhat/Documents/webhosting/wwp-inventory

# Инициализация
git init

# Добавление файлов
git add .

# Коммит
git commit -m "Initial commit"

# Настройка remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/wwp-inventory.git

# Push в GitHub
git branch -M main
git push -u origin main
```

### Шаг 3: Быстрое развертывание

**Вариант A: Автоматический скрипт**

```bash
# 1. Отредактируйте deploy/first-deploy.sh
# Укажите GIT_REPO_URL в начале файла

# 2. Запустите скрипт
chmod +x deploy/first-deploy.sh
export GIT_REPO_URL="https://github.com/YOUR_USERNAME/wwp-inventory.git"
./deploy/first-deploy.sh
```

**Вариант B: Ручное развертывание (см. QUICK_DEPLOY.md)**

### Шаг 4: Настройка DNS

Настройте DNS записи для домена `ams.it-uae.com`:

```
A     ams.it-uae.com         88.99.124.218
A     www.ams.it-uae.com     88.99.124.218
```

### Шаг 5: Установка SSL

После настройки DNS:

```bash
ssh root@88.99.124.218
certbot --nginx -d ams.it-uae.com -d www.ams.it-uae.com --non-interactive --agree-tos --email rakhat.utebayev@gmail.com --redirect
```

### Шаг 6: Проверка

Откройте в браузере: `https://ams.it-uae.com`

## 📝 Важные замечания

1. **Пароль сервера:** После первого развертывания настройте SSH ключи для безопасности
2. **Пароли БД:** Сохраните пароли из файла `.env` на сервере в безопасном месте
3. **Существующие системы:** Проект развернут в `/opt/wwp-ams` и не затрагивает другие системы
4. **Порты:** Backend работает на 127.0.0.1:8000, Frontend на 127.0.0.1:3000 (внутренние)

## 🔄 Обновление

После изменений в коде:

```bash
# На вашем компьютере
git add .
git commit -m "Update"
git push

# На сервере
ssh root@88.99.124.218
cd /opt/wwp-ams
git pull
docker-compose build
docker-compose up -d
```

## 📚 Дополнительная документация

- `QUICK_DEPLOY.md` - Подробная инструкция по ручному развертыванию
- `DEPLOY.md` - Полная документация по развертыванию

